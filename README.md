# User Hub
### React Dashboard with auth pages
A clean, minimal user dashboard built with React and Typescript. Includes authentication pages and sample integrations so you can showcase a real-feeling app in your frontend portfolio. mainly for frontend, you can ignore the API implementations

### Key Features

```
- Dummy data
- User & Auth state managed via React Context
- Authentication pages: Login, Signup, Forgot/Reset Password, Verify
- Crypto payment gateway hooks for Bitcoin, ETH, etc. (integration points — not production wallets)
- Responsive layout with sidebar, Bottom Navigation Bar (mobile) and mobile navigation

```